{
  "name": "typescript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist/ && tsc",
    "test": "npm run build && node nightwatch -e firefox,chrome,edge && npm run cleanup",
    "cleanup": "taskkill /f /im chromedriver.exe & taskkill /f /im msedge.exe & taskkill /f /im geckodriver.exe"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nightwatch": "^1.5.1",
    "ts-loader": "^8.0.11"
  },
  "devDependencies": {
    "@types/node": "^14.14.7"
  }
}
